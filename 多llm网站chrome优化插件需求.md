

# 豆包对话增强扩展 — 需求规格说明书

---

## 一、项目概述

| 项 | 说明 |
|---|---|
| 产品形态 | Chrome 浏览器扩展（Manifest V3） |
| 首期适配 | 豆包（www.doubao.com） |
| 架构要求 | DOM 解析层独立模块，后续可替换适配其他大模型网站 |
| 核心原则 | **不删除、不隐藏、不大幅移动**原有页面 DOM 元素，仅在合适位置**插入**新节点 |
| 样式隔离 | 所有新增 CSS class 使用 `dce-` 前缀，不污染源站样式 |

---

## 二、功能模块总览

```
┌─────────────────────────────────────────────────────────┐
│                    豆包页面                               │
│  ┌──────────┐  ┌──────────────────────┐  ┌───────────┐  │
│  │ 原有侧边栏 │  │                      │  │ 右侧导航   │  │
│  │          │  │                      │  │ 预览条     │  │
│  │ ┌──────┐ │  │                      │  │           │  │
│  │ │功能区 │ │  │                      │  │  · 问题1   │  │
│  │ └──────┘ │  │     对话内容区         │  │  ● 问题2   │  │
│  │ ┌──────┐ │  │                      │  │  · 问题3   │  │
│  │ │★分组  │ │  │                      │  │  · 问题4   │  │
│  │ │ 面板  │ │  │                      │  │           │  │
│  │ └──────┘ │  │                      │  │  ← 滚动条  │  │
│  │ ┌──────┐ │  │                      │  └───────────┘  │
│  │ │历史   │ │  │                      │     [导出按钮]→右上 │
│  │ │对话列表│ │  │                      │                │
│  │ └──────┘ │  └──────────────────────┘                │
│  └──────────┘                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 三、功能一：左侧对话分组管理

### 3.1 面板位置

| 项 | 说明 |
|---|---|
| 嵌入位置 | 豆包原有左侧边栏**内部** |
| 上边界 | 侧边栏顶部功能区（新建对话按钮等）**下方** |
| 下边界 | 历史对话列表**上方** |
| 实现方式 | `insertBefore` 插入新 DOM 节点，不使用浮层/fixed 定位 |

### 3.2 分组 CRUD

| 操作 | 交互方式 | 说明 |
|------|---------|------|
| 创建分组 | 点击 `+` 按钮 → 弹出行内输入框 | 输入名称 + 选择颜色标签 |
| 删除分组 | 右键菜单 或 hover 出现 `×` 按钮 | 分组内对话自动移回「未分组」，需二次确认 |
| 重命名分组 | 双击分组名 → 变为可编辑输入框 | 最大 20 字符 |
| 修改颜色 | 右键菜单 → 颜色选择器 | 提供 12 种预设色 |
| 折叠/展开 | 点击分组标题左侧箭头 | 折叠时只显示分组名和对话数量 |
| 全部折叠 | 面板标题栏按钮 | 一键折叠所有分组 |

### 3.3 对话条目

| 项 | 说明 |
|---|---|
| 显示内容 | 对话标题（前 **16 字符** + `...`）+ 相对时间 |
| 激活标识 | 当前打开的对话条目高亮显示 |
| 未归组对话 | 自动放入「未分组」分组 |
| 点击行为 | 点击对话条目 → 跳转到该对话（触发豆包原有的对话切换） |

### 3.4 拖拽能力

| 拖拽场景 | 说明 |
|---------|------|
| **对话拖入分组** | 从任意位置拖拽一条对话条目，放到目标分组上 → 对话归入该分组 |
| **对话在分组间移动** | 从分组 A 拖出放到分组 B |
| **分组排序** | 拖拽分组标题栏上下移动，调整分组显示顺序 |
| 视觉反馈 | 拖拽时显示半透明拖影 + 目标分组高亮放置区 |

### 3.5 存储

| 项 | 说明 |
|---|---|
| 存储方式 | `chrome.storage.local`（本地） |
| 存储内容 | 分组列表（id/名称/颜色/顺序/折叠状态）、每个分组包含的对话 ID 列表、对话→分组映射表 |
| 预留接口 | 存储层做抽象封装，暴露 `sync()` 接口，后续可对接云端 |

---

## 四、功能二：右侧对话导航预览

### 4.1 面板位置与布局

| 项 | 说明 |
|---|---|
| 位置 | 页面右侧，紧贴对话内容区右边缘 |
| **不遮挡滚动条** | 面板在浏览器原生滚动条的**左侧** |
| 形态 | 窄条面板（宽度约 180-220px） |
| 折叠 | 可点击收起，收起后只保留一个小图标按钮 |

### 4.2 导航点规则

| 项 | 说明 |
|---|---|
| 每个点代表 | 一次**用户提问**（不包含 AI 回复） |
| 点的显示文字 | 提问内容前 **16 个字符**，超出用 `...` 截断 |
| 点的数量 | 与当前对话中的用户提问数 1:1 对应 |

### 4.3 交互行为

| 交互 | 说明 |
|------|------|
| 点击导航点 | **平滑滚动**到对话内容区中对应提问的位置 |
| 滚动联动 | 用户手动滚动对话内容时，导航面板中**实时高亮**当前可视区域对应的导航点 |
| 对话切换 | 切换到不同对话后，导航点列表**自动刷新** |

### 4.4 不做的

- 不显示 hover 摘要弹窗（导航点本身已有截断文字）
- 不显示 AI 回复的导航点

---

## 五、功能三：对话导出

### 5.1 入口

| 项 | 说明 |
|---|---|
| 按钮位置 | 页面**右上角** |
| 按钮形态 | 图标按钮（下载图标），hover 显示文字提示 |
| 点击行为 | 弹出格式选择下拉菜单（HTML / PDF / Markdown） |

### 5.2 导出范围

- 导出**当前对话**的**全部轮次**（所有用户提问 + 所有 AI 回复）
- 保留原始对话顺序

### 5.3 三种格式详情

#### 5.3.1 HTML 导出

| 项 | 说明 |
|---|---|
| 输出 | 单个 `.html` 文件 |
| 图片 | 下载后转 **base64** 内嵌到 HTML |
| 代码块 | 保留**语法高亮**样式 |
| 样式 | 内嵌完整 CSS，离线可直接打开 |
| 排版 | 用户消息靠右/蓝色标识，AI 消息靠左/绿色标识 |

#### 5.3.2 PDF 导出

| 项 | 说明 |
|---|---|
| 输出 | 单个 `.pdf` 文件 |
| 图片 | 内嵌到 PDF |
| 代码块 | 保留语法高亮 |
| 实现方式 | 先生成 HTML → 使用浏览器 print API 或 html2canvas + jsPDF 转换 |

#### 5.3.3 Markdown 导出

| 项 | 说明 |
|---|---|
| 输出 | 一个**文件夹**（以对话标题命名） |
| 文件夹结构 | 见下方 |
| 图片 | **下载为文件**，存放在子文件夹 `images/` 中 |
| 图片引用 | Markdown 中使用**相对路径** `![描述](images/img_001.png)` |
| 代码块 | 使用标准 Markdown 代码块语法（带语言标识） |

```
对话标题/
├── 对话标题.md
└── images/
    ├── img_001.png
    ├── img_002.jpg
    └── ...
```

### 5.4 导出文件命名

| 项 | 规则 |
|---|---|
| 文件名 | `{对话标题}_{导出日期}.{格式后缀}` |
| 对话标题来源 | 取对话中第一条用户提问的前 16 字符 |
| 特殊字符处理 | 文件名中的 `/\:*?"<>|` 替换为 `_` |

---

## 六、技术要求

### 6.1 架构分层

```
┌─────────────────────────────────┐
│          main.js（入口协调）       │
├──────┬──────┬──────┬────────────┤
│ DOM  │ 分组  │ 导航  │  导出       │
│ 解析 │ 管理  │ 预览  │  模块       │
│ 层   │ 模块  │ 模块  │            │
├──────┴──────┴──────┴────────────┤
│         storage.js（存储抽象层）    │
└─────────────────────────────────┘
```

### 6.2 DOM 解析层要求

| 项 | 说明 |
|---|---|
| 集中管理选择器 | 所有豆包 DOM 选择器集中在一个配置对象中 |
| 多策略降级 | 先尝试精确选择器 → 再通过布局特征扫描 → 兜底策略 |
| 适配性考虑 | 豆包 DOM class 可能含动态 hash，优先用结构特征/属性选择器 |
| 模块可替换 | 后续适配其他网站时，只需新写一个 Parser，其他模块不变 |

### 6.3 性能要求

| 项 | 说明 |
|---|---|
| DOM 监听 | 使用 MutationObserver，带 300ms 防抖 |
| 滚动监听 | 使用 `requestAnimationFrame` 节流 |
| 存储写入 | 使用防抖，避免频繁写入 |
| 渲染 | 分组列表变更时局部更新，不全量重绘 |

### 6.4 样式隔离

| 项 | 说明 |
|---|---|
| CSS 前缀 | 所有新增 class 使用 `dce-` 前缀 |
| 不使用全局标签选择器 | 避免 `div {}` `p {}` 等污染全局 |
| z-index 管理 | 新增浮层类元素 z-index 统一管理，不超过 10000 |

---

## 七、当前版本不做的事

| 不做 | 原因 |
|------|------|
| 云端同步 | 预留接口，后续版本实现 |
| 多网站适配 | 首期只做豆包 |
| 对话内容搜索 | 后续版本 |
| 对话合并/拆分 | 后续版本 |
| 自定义快捷键 | 后续版本 |
| 批量导出所有对话 | 后续版本（当前只导出当前对话） |

---

## 八、验收标准

| 编号 | 验收项 | 标准 |
|------|-------|------|
| A1 | 安装后豆包页面功能正常 | 原有对话列表、新建对话、发送消息等功能不受影响 |
| A2 | 分组面板正确嵌入 | 位于功能区下方、对话列表上方，不遮挡不错位 |
| A3 | 创建/删除/重命名分组 | 操作后立即生效，刷新页面后数据保持 |
| A4 | 拖拽对话到分组 | 拖拽流畅，有视觉反馈，松开后对话归入目标分组 |
| A5 | 拖拽分组排序 | 拖拽分组可调整上下顺序，松开后顺序保持 |
| A6 | 右侧导航点正确 | 导航点数量 = 用户提问数，文字为前16字符 |
| A7 | 点击导航点跳转 | 平滑滚动到对应提问位置 |
| A8 | 滚动联动高亮 | 手动滚动时当前位置对应的导航点实时高亮 |
| A9 | 导航面板不遮挡滚动条 | 浏览器原生滚动条完全可见可操作 |
| A10 | HTML 导出 | 单文件，图片 base64 内嵌，代码块有高亮，离线可打开 |
| A11 | PDF 导出 | 单文件，图片和代码块正常渲染 |
| A12 | MD 导出 | 文件夹结构正确，图片使用相对路径，预览工具可正常渲染 |

---
A2分组面板正确嵌入位于原左侧栏功能区下方、对话列表上方，不遮挡不错位

> **以上为全部需求，确认无误后我开始编码实现。**
